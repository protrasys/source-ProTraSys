import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
  center: {
    textAlign: 'center',
    padding: '2rem',
  },
  img: {
    width: '100%',
    height: 250,
  },
}));

export default useStyles;
