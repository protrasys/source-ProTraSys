import React, { Component } from 'react';
import { withStyles } from '@material-ui/core';
import useStyles from './Style';

class StudentControlPanel extends Component {
  render() {
    return <div>Student Control Panel</div>;
  }
}

export default withStyles(useStyles)(StudentControlPanel);
